'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var util = require('util');

function grpcId(ipfs, options = {}) {
  function id(request, metadata) {
    const opts = {
      ...request,
      ...metadata
    };
    return ipfs.id(opts);
  }
  return util.callbackify(id);
}

exports.grpcId = grpcId;
