'use strict';

var resources = require('ipfs-http-gateway/resources/index');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var resources__default = /*#__PURE__*/_interopDefaultLegacy(resources);

const webuiCid = 'bafybeif4zkmu7qdhkpf3pnhwxipylqleof7rl6ojbe7mq3fzogz6m4xk3i';
var webuiRoutes = [
  {
    method: 'GET',
    path: `/ipfs/${ webuiCid }/{path*}`,
    options: {
      handler: resources__default["default"].gateway.handler,
      response: { ranges: false },
      ext: { onPostHandler: { method: resources__default["default"].gateway.afterHandler } }
    }
  },
  {
    method: 'GET',
    path: '/webui/{slug?}',
    handler(_request, h) {
      return h.redirect(`/ipfs/${ webuiCid }/`);
    }
  }
];

module.exports = webuiRoutes;
