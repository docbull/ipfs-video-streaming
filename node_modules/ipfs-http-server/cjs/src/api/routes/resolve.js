'use strict';

var resolve = require('../resources/resolve.js');

var resolveRoutes = [{
    method: 'POST',
    path: '/api/v0/resolve',
    ...resolve.resolveResource
  }];

module.exports = resolveRoutes;
