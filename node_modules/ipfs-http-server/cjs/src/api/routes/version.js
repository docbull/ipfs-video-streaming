'use strict';

var version = require('../resources/version.js');

var versionRoutes = [{
    method: 'POST',
    path: '/api/v0/version',
    ...version.versionResource
  }];

module.exports = versionRoutes;
