'use strict';

var shutdown = require('../resources/shutdown.js');

var shutdownRoutes = [{
    method: 'POST',
    path: '/api/v0/shutdown',
    ...shutdown.shutdownResource
  }];

module.exports = shutdownRoutes;
