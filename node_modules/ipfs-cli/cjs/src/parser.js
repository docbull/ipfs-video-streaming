'use strict';

var yargs = require('yargs');
var utils = require('./utils.js');
var index = require('./commands/index.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var yargs__default = /*#__PURE__*/_interopDefaultLegacy(yargs);

const args = yargs__default["default"](process.argv.slice(2)).option('silent', {
  desc: 'Write no output',
  type: 'boolean',
  default: false,
  coerce: silent => {
    if (silent)
      utils.disablePrinting();
    return silent;
  }
}).option('pass', {
  desc: 'Pass phrase for the keys',
  type: 'string',
  default: ''
}).option('migrate', {
  desc: 'Enable/disable automatic repo migrations',
  type: 'boolean',
  default: false
}).options('api', {
  desc: 'Remote API multiaddr to use',
  type: 'string'
}).epilog(utils.ipfsPathHelp).demandCommand(1, 'Please specify a command').showHelpOnFail(false).command(index.commandList).help().strict().completion();

module.exports = args;
