'use strict';

var parseDuration = require('parse-duration');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var pubsubPeers = {
  command: 'peers <topic>',
  describe: 'Get all peers subscribed to a topic',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    topic,
    timeout
  }) {
    const peers = await ipfs.pubsub.peers(topic, { timeout });
    peers.forEach(peer => print(peer));
  }
};

module.exports = pubsubPeers;
