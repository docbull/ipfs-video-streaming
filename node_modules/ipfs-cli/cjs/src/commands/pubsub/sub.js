'use strict';

var parseDuration = require('parse-duration');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var pubsubSub = {
  command: 'sub <topic>',
  describe: 'Subscribe to a topic',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    topic,
    timeout
  }) {
    const handler = msg => print(msg.data.toString());
    await ipfs.pubsub.subscribe(topic, handler, { timeout });
  }
};

module.exports = pubsubSub;
