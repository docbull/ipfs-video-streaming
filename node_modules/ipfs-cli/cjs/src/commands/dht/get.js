'use strict';

var parseDuration = require('parse-duration');
var utils = require('../../utils.js');
var toString = require('uint8arrays/to-string');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var dhtGet = {
  command: 'get <key>',
  describe: 'Given a key, query the routing system for its best value.',
  builder: {
    key: {
      type: 'string',
      coerce: utils.coerceCID
    },
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    key,
    timeout
  }) {
    for await (const event of await ipfs.dht.get(key.bytes, { timeout })) {
      if (event.name === 'VALUE') {
        print(toString.toString(event.value, 'base58btc'));
      }
    }
  }
};

module.exports = dhtGet;
