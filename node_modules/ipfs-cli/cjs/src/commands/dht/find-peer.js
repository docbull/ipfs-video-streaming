'use strict';

var parseDuration = require('parse-duration');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var dhtFindPeer = {
  command: 'findpeer <peerId>',
  describe: 'Find the multiaddresses associated with a Peer ID.',
  builder: {
    peerId: { type: 'string' },
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    peerId,
    timeout
  }) {
    for await (const event of ipfs.dht.findPeer(peerId, { timeout })) {
      if (event.name === 'FINAL_PEER') {
        event.peer.multiaddrs.forEach(addr => print(`${ addr }`));
      }
    }
  }
};

module.exports = dhtFindPeer;
