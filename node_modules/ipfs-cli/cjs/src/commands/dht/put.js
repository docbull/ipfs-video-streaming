'use strict';

var parseDuration = require('parse-duration');
var fromString = require('uint8arrays/from-string');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var dhtPut = {
  command: 'put <key> <value>',
  describe: 'Write a key/value pair to the routing system.',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs},
    key,
    value,
    timeout
  }) {
    await ipfs.dht.put(fromString.fromString(key), fromString.fromString(value), { timeout });
  }
};

module.exports = dhtPut;
