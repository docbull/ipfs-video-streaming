'use strict';

var parseDuration = require('parse-duration');
var cid = require('multiformats/cid');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var dagExport = {
  command: 'export <root cid>',
  describe: 'Streams the DAG beginning at the given root CID as a CAR stream on stdout.',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    rootcid,
    timeout
  }) {
    const options = { timeout };
    const cid$1 = cid.CID.parse(rootcid);
    const exporter = ipfs.dag.export(cid$1, options);
    for await (const chunk of exporter) {
      print.write(chunk);
    }
  }
};

module.exports = dagExport;
