'use strict';

var index = require('./dag/index.js');

var dag = {
  command: 'dag <command>',
  description: 'Interact with ipld dag objects.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = dag;
