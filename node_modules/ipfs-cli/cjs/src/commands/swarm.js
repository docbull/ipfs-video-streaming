'use strict';

var index = require('./swarm/index.js');

var swarm = {
  command: 'swarm <command>',
  description: 'Swarm inspection tool.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = swarm;
