'use strict';

var index = require('./pubsub/index.js');

var namePubsub = {
  command: 'pubsub',
  description: 'IPNS pubsub management.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = namePubsub;
