'use strict';

var parseDuration = require('parse-duration');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var namePubsubState = {
  command: 'state',
  describe: 'Query the state of IPNS pubsub.',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    timeout
  }) {
    const result = await ipfs.name.pubsub.state({ timeout });
    print(result.enabled ? 'enabled' : 'disabled');
  }
};

module.exports = namePubsubState;
