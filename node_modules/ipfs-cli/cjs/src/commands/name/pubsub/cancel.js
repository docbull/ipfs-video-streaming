'use strict';

var parseDuration = require('parse-duration');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var namePubsubCancel = {
  command: 'cancel <name>',
  describe: 'Cancel a name subscription.',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    name,
    timeout
  }) {
    const result = await ipfs.name.pubsub.cancel(name, { timeout });
    print(result.canceled ? 'canceled' : 'no subscription');
  }
};

module.exports = namePubsubCancel;
