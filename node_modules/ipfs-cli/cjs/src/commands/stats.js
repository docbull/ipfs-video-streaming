'use strict';

var index = require('./stats/index.js');

var stats = {
  command: 'stats <command>',
  description: 'Query IPFS statistics.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = stats;
