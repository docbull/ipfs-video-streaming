'use strict';

var index = require('./object/index.js');

var object = {
  command: 'object <command>',
  description: 'Interact with ipfs objects.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = object;
