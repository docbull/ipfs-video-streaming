'use strict';

var index = require('./cid/index.js');

var cid = {
  command: 'cid <command>',
  description: 'Convert, format and discover properties of CIDs.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = cid;
