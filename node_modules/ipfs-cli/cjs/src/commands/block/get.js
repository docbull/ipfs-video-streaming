'use strict';

var parseDuration = require('parse-duration');
var toString = require('uint8arrays/to-string');
var utils = require('../../utils.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var blockGet = {
  command: 'get <key>',
  describe: 'Get a raw IPFS block',
  builder: {
    key: {
      type: 'string',
      coerce: utils.coerceCID
    },
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({ctx, key, timeout}) {
    const {ipfs, print} = ctx;
    const block = await ipfs.block.get(key, { timeout });
    if (block) {
      print(toString.toString(block), false);
    } else {
      print('Block was unwanted before it could be remotely retrieved');
    }
  }
};

module.exports = blockGet;
