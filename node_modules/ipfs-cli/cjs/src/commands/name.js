'use strict';

var index = require('./name/index.js');

var name = {
  command: 'name <command>',
  description: 'Publish and resolve IPNS names.',
  builder(yargs) {
    index.commands.forEach(command => {
      yargs.command(command);
    });
    return yargs;
  }
};

module.exports = name;
