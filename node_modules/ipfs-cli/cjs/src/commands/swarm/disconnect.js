'use strict';

var parseDuration = require('parse-duration');
var utils = require('../../utils.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var swarmDisconnect = {
  command: 'disconnect <address>',
  describe: 'Close connection to a given address',
  builder: {
    address: {
      type: 'string',
      coerce: utils.coerceMultiaddr
    },
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, isDaemon, print},
    address,
    timeout
  }) {
    if (!isDaemon) {
      throw new Error('This command must be run in online mode. Try running \'ipfs daemon\' first.');
    }
    await ipfs.swarm.disconnect(address, { timeout });
    print(`${ address }`);
  }
};

module.exports = swarmDisconnect;
