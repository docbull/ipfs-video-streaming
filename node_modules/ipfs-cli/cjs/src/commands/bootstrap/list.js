'use strict';

var parseDuration = require('parse-duration');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var bootstrapList = {
  command: 'list',
  describe: 'Show peers in the bootstrap list',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    timeout
  }) {
    const list = await ipfs.bootstrap.list({ timeout });
    list.Peers.forEach(node => print(node.toString()));
  }
};

module.exports = bootstrapList;
