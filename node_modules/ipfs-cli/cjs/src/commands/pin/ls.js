'use strict';

var parseDuration = require('parse-duration');
var utils = require('../../utils.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var pinLs = {
  command: 'ls [ipfsPath...]',
  describe: 'List objects pinned to local storage.',
  builder: {
    type: {
      type: 'string',
      alias: 't',
      default: 'all',
      choices: [
        'direct',
        'indirect',
        'recursive',
        'all'
      ],
      describe: 'The type of pinned keys to list.'
    },
    quiet: {
      type: 'boolean',
      alias: 'q',
      default: false,
      describe: 'Write just hashes of objects.'
    },
    'cid-base': {
      describe: 'Number base to display CIDs in.',
      type: 'string',
      default: 'base58btc'
    },
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  async handler({
    ctx: {ipfs, print},
    ipfsPath,
    type,
    quiet,
    cidBase,
    timeout
  }) {
    const base = await ipfs.bases.getBase(cidBase);
    const printPin = res => {
      let line = res.cid.toString(base.encoder);
      if (!quiet) {
        line += ` ${ res.type }`;
        if (res.metadata) {
          line += ` ${ JSON.stringify(utils.makeEntriesPrintable(res.metadata, base)) }`;
        }
      }
      print(line);
    };
    for await (const res of ipfs.pin.ls({
        paths: ipfsPath,
        type,
        timeout
      })) {
      printPin(res);
    }
  }
};

module.exports = pinLs;
