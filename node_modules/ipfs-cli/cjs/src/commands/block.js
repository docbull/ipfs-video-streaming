'use strict';

var index = require('./block/index.js');

var block = {
  command: 'block <command>',
  description: 'Manipulate raw IPFS blocks.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = block;
