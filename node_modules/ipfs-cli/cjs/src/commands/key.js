'use strict';

var index = require('./key/index.js');

var key = {
  command: 'key <command>',
  description: 'Manage your keys',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = key;
