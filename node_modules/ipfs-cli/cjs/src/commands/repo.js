'use strict';

var index = require('./repo/index.js');

var repo = {
  command: 'repo <command>',
  description: 'Manipulate the IPFS repo.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = repo;
