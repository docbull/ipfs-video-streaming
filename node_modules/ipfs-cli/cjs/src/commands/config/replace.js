'use strict';

var path = require('path');
var fs = require('fs');
var parseDuration = require('parse-duration');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var path__default = /*#__PURE__*/_interopDefaultLegacy(path);
var fs__default = /*#__PURE__*/_interopDefaultLegacy(fs);
var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);

var configReplace = {
  command: 'replace <file>',
  describe: 'Replaces the config with <file>',
  builder: {
    timeout: {
      type: 'string',
      coerce: parseDuration__default["default"]
    }
  },
  handler({
    ctx: {ipfs, isDaemon},
    file,
    timeout
  }) {
    const filePath = path__default["default"].resolve(process.cwd(), file);
    const config = isDaemon ? filePath : JSON.parse(fs__default["default"].readFileSync(filePath, 'utf8'));
    return ipfs.config.replace(config, { timeout });
  }
};

module.exports = configReplace;
