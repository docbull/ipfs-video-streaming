'use strict';

var index = require('./profile/index.js');

var configProfile = {
  command: 'profile <command>',
  description: 'Interact with config profiles.',
  builder(yargs) {
    return yargs.command(index.commands);
  }
};

module.exports = configProfile;
