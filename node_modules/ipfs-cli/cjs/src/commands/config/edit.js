'use strict';

var path = require('path');
var execa = require('execa');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var path__default = /*#__PURE__*/_interopDefaultLegacy(path);
var execa__default = /*#__PURE__*/_interopDefaultLegacy(execa);

var configEdit = {
  command: 'edit',
  describe: 'Opens the config file for editing in $EDITOR',
  async handler(argv) {
    const editor = process.env.EDITOR;
    if (!editor) {
      throw new Error('ENV variable $EDITOR not set');
    }
    await execa__default["default"](editor, [path__default["default"].join(argv.ctx.repoPath, 'config')]);
  }
};

module.exports = configEdit;
