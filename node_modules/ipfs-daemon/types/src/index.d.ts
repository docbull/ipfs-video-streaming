export class Daemon {
    /**
     * @param {import('ipfs-core').Options} options
     */
    constructor(options?: import('ipfs-core').Options);
    _options: import("ipfs-core/src/types").Options;
    /** @type {import('ipfs-core-types').IPFS} */
    _ipfs: import('ipfs-core-types').IPFS;
    /**
     * Starts the IPFS HTTP server
     */
    start(): Promise<void>;
    _httpApi: HttpApi | undefined;
    _httpGateway: HttpGateway | undefined;
    _grpcServer: import("ipfs-grpc-server/types/src/types").WebsocketServer | undefined;
    stop(): Promise<void>;
}
import { HttpApi } from "ipfs-http-server";
import { HttpGateway } from "ipfs-http-gateway";
//# sourceMappingURL=index.d.ts.map